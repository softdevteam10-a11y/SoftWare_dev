# ---------------------------------------------------------------------------
# Driver Subsystem — environment configuration
# Copy this file to ".env" and fill in real values. Never commit ".env".
# ---------------------------------------------------------------------------

PORT=4004

# --- Local Driver Database (owned exclusively by this subsystem) ----------
PGHOST=localhost
PGPORT=5432
PGDATABASE=salone_clean_driver
PGUSER=postgres
PGPASSWORD=change_me

# --- API Gateway ------------------------------------------------------------
# This is the ONLY door through which this subsystem reaches the Customer
# Subsystem or Management Subsystem — e.g. to report a completed pickup or
# raise a "bin full" alert. It never queries their databases directly.
#
# For LOCAL TESTING, point this at the mock gateway used by the other two
# subsystems (salone-clean-mock-gateway) — see that project's README:
API_GATEWAY_BASE_URL=http://localhost:4003/api/v1
# In production, swap this for the real Gateway's URL, e.g.:
# API_GATEWAY_BASE_URL=https://gateway.saloneclean.sl/api/v1
API_GATEWAY_SERVICE_KEY=local-test-key

CORS_ORIGIN=*
